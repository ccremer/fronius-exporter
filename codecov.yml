# This file is managed by greposync.
# Do not modify manually.
# Adjust variables in `.sync.yml`.
comment: false
coverage:
  status:
    patch:
      default:
        threshold: 50%
    project:
      default:
        threshold: 10%
